# Generated by Cargo
# will have compiled files and executables
debug
target

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# Generated by cargo mutants
# Contains mutation testing data
**/mutants.out*/
# RustRover
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/
# Unreal Engine Project Files
*.sln
*.slnx
*.suo
*.xcodeproj
*.xcworkspace
.vs/
.vsconfig
.vscode/settings.json.user

# Unreal Engine Build Artifacts
Binaries/
Build/
DerivedDataCache/
Intermediate/
Saved/

# Compiled Object files
*.slo
*.lo
*.o
*.obj
*.ko
*.elf

# Precompiled Headers
*.gch
*.pch

# Compiled Dynamic libraries
*.so
*.dylib
*.dll

# Compiled Static libraries
*.lai
*.la
*.a
*.lib

# Executables
*.exe
*.out
*.app
*.ipa
*.apk

# Debug files
*.dSYM/
*.su
*.idb
*.pdb

# Unreal Engine cache files
DerivedDataCache/

# Unreal Engine autosave files
*_BuiltData.uasset
*.autosave

# Unreal Engine crash logs
/Saved/Logs/
/Saved/Crashes/
/Saved/Autosaves/

# Unreal Engine shader cache
/Saved/ShaderDebugInfo/

# Visual Assist
*.ncb
*.sdf
*.opensdf
*.VC.db
*.VC.VC.opendb

# Visual Studio Code
.vscode/settings.json.user
.history/

# JetBrains Rider
.idea/
*.sln.iml

# OS Files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
*~

# Temporary files
*.swp
*.swo
*.tmp
*.bak
*.cache

# Starter Content (uncomment if you don't want it in repo)
# /Content/StarterContent/

# C++ intermediate files
*.ipch
*.user
*.suo
*.opensdf
*.sdf
*.VC.opendb
*.VC.db

# Package files (can be large)
*.pak

# Video files (if recorded in-editor)
*.avi
*.mp4
*.mov

# Audio temp files
*.wav.reapeaks

# Build folders from external tools
/Build/Linux/
/Build/Windows/
/Build/Mac/
/Build/Android/
/Build/IOS/

# Plugin intermediate files
Plugins/**/Binaries/
Plugins/**/Intermediate/

# Local config files that might contain sensitive data
/Config/UserSettings.ini
/Saved/Config/

# Node modules (if using any web tools)
node_modules/

# Python cache (if using Python scripts)
__pycache__/
*.pyc

# Unreal Insights
*.utrace
/Saved/Profiling/

# Live Coding
/Saved/LiveCoding/

# Android/Mobile builds
/Build/Android/
*.apk
*.obb
*.aab

# iOS builds
/Build/IOS/
*.ipa
*.dSYM

# Linux builds
/Build/Linux/

# Documentation build outputs
/Docs/Output/
/Docs/Build/
certs/
.venv/

# Procedural terrain cache files (generated)
*.terrain.msgpack
# Generated Unreal track exports (bake with: cargo run --bin ats-export -- --all)
content/tracks/export/
# Generated server sidecars — the ground heightfield, the curb bands and
# the walls — written next to each track YAML by the same ats-export run
content/tracks/real/*.ground.msgpack
content/tracks/real/*.curbs.msgpack

# Recorded session replays (large binary, generated at runtime)
server/replays/

# Lap records and their ghost traces (written at runtime)
server/records/

# Scratch/working directory, not part of the project
_claude_tmp/
content/tracks/real/*.walls.msgpack
# Raw OpenStreetMap extracts the layout dossiers are built from
# (scripts/osm_layout.py). Tens of MB, refetchable, and the dossier that
# comes out of them is what is checked in.
content/tracks/osm-cache/
# Raw Copernicus GLO-30 DEM tiles the terrain sidecars are built from
# (scripts/dem_fetch.py). ~30 MB per 1-degree tile, refetchable from the
# public AWS bucket, and the sidecar that comes out of them is the artefact
# that matters.
content/tracks/dem-cache/
# The elevation sidecar itself (content/tracks/real/*.dem.msgpack) is NOT
# ignored, unlike the ground/curbs/walls sidecars beside it. Those are
# derived from files in the repo and any build can regenerate them; this
# one needs a few hundred megabytes off the network, so it is checked in
# and the download is a one-off for whoever refreshes it.
# Build outputs: the packaged client (scripts/build_game_standalone.ps1)
# and the release packages and zips (scripts/build_release.ps1)
artifacts/
# Startup settings, written by the client on first run. Describes this machine
# (its display, the server it points at), so it is never shared.
/game-unreal/settings.yml
game-unreal/Content/

Claude outputs/*
server/target*/
artifacts/
artifacts/*
# Baked tiling ground textures (python scripts/bake_ground_textures.py).
# Deterministic from seeds, ~20 MB of noise that compresses badly, and what
# the client uses is the /Game/Ground import made from them.
content/textures/ground/
